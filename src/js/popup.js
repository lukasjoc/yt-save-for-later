"use strict"

let target = document.getElementById("saveVideo")

target.onclick = function(e) {
	let loc = document.querySelector(".ytp-time-current")
	let host = window.location
	document.write(loc + "\n")
	document.write(url)
}
